# Wings / Tenders Category Cleanup Proposal

**Scope:** 2,007 canonicals containing `wing`/`wings`/`tender`/`tenders` (of 114,855). Reviewed top ~280 by count + token-substitution sweeps.

**Actions:** 9 merges, 3 renames, 22 judgment flags.

## Top-5

| cid | canonical | count |
|----:|-----------|------:|
| 656 | boneless wings | 2,587 |
| 657 | handcrafted tenders | 2,573 (Cane's) |
| 661 | chicken tenders | 1,886 |
| 670 | traditional wings | 1,375 |
| 683 | bone wings | 1,006 |

## Merges

- **`barbecue/barbeque → bbq`:** `barbecue wings` (5145) and `barbeque wing` (21907) → `bbq wings` (925).
- **Filler `only`:** five clusters collapse to non-`only` siblings — `chicken/express/handcrafted only tenders` and `hot/boneless only wings`.
- **`only tenders` (22)** → `chicken tenders` (medium): bare `tenders` defaults to chicken tenders.
- **Singular→plural:** `jet wing` (2) → `jet wings` (8).

## Renames

`smokey → smoky` for `bbq smokey wings` (19), `bbq smokey texas wings` (2), `fried order smokey wings` (1). No `smoky` siblings.

## Held back as judgment

- **High-impact:** `tenders` (810), `crispy tenders` (223), `chicken crispy tenders` (362) — likely = `chicken tenders` but bare `tenders` may include steak; `crispy tenders` is a brand line.
- **No target:** `only wings` (81); orphan `barbecue X wings` (chipotle/boneless/classic) — no `bbq X wings` siblings.
- **BWW `things` clusters** (4 × ~56) — "Wild Things" brand artifact.
- **`wing wings`** double-token raw-text artifacts.
- **Combos:** `tenders wings`, `or tenders wings`.
- **Strip/finger ≡ tender synonyms** (small counts).
- **`plain` (50) vs `naked` (143)** wings — subtle distinction.
- **`bone less wings`** space-typo — should route to `boneless`, not `bone`; dirty cluster.

## Explicitly NOT merged

`bone` vs `boneless` (format), `traditional wings` (bone-in indicator), brand items (Cane's, Popeyes, KFC, BJ's, Hooters), distinct flavors/cuts (`all drums`, `all flappers`, cauliflower, Beyond), sandwich forms (`... sandwich tender` = sandwich).

No `bf → buffalo` matches found.
